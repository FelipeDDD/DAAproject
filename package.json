{
  "name": "daaproject",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:multiplayer": "node --env-file=.env.local scripts/check-multiplayer.mjs",
    "test:doors-online": "node --env-file=.env.local scripts/check-shared-doors.mjs",
    "test:characters": "node --env-file=.env.local scripts/check-characters.mjs",
    "test:chat": "node --env-file=.env.local scripts/check-chat.mjs",
    "dev": "vite",
    "convex": "convex dev",
    "preconvex": "node scripts/sync-door-definitions.mjs",
    "prebuild": "node scripts/sync-door-definitions.mjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "convex": "^1.45.0",
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
